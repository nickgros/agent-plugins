{
  "name": "agent-config",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "skills:sync": "pnpx skills update -p && cp -r .agents/skills/* skills/ && rm -rf .agents .claude"
  },
  "packageManager": "pnpm@11.25.0"
}